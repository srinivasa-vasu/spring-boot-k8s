apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  starter.hello: "Hello there! from configtree structure"
  starter.delay: "2000"
  app.yaml: |
    starter:
      hello: "Hello there! from file structure"
      delay: "3000"